import { Directive3Directive } from './directive3.directive';

describe('Directive3Directive', () => {
  it('should create an instance', () => {
    const directive = new Directive3Directive();
    expect(directive).toBeTruthy();
  });
});
